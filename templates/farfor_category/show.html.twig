{% extends 'base.html.twig' %}

{% block title %}FarforCategory{% endblock %}

{% block body %}
    <h1>FarforCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farfor_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ farfor_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('farfor_category_index') }}">back to list</a>

    <a href="{{ path('farfor_category_edit', {'id': farfor_category.id}) }}">edit</a>

    {{ include('farfor_category/_delete_form.html.twig') }}
{% endblock %}
