{% extends 'base.html.twig' %}

{% block title %}Edit Farfor{% endblock %}

{% block body %}
    <h1>Edit Farfor</h1>

    {{ include('farfor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('farfor_index') }}">back to list</a>

    {{ include('farfor/_delete_form.html.twig') }}
{% endblock %}
