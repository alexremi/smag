{% extends 'base.html.twig' %}

{% block title %}Farfor{% endblock %}

{% block body %}
    <h1>Farfor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farfor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ farfor.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ farfor.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('farfor_index') }}">back to list</a>

    <a href="{{ path('farfor_edit', {'id': farfor.id}) }}">edit</a>

    {{ include('farfor/_delete_form.html.twig') }}
{% endblock %}
