{% extends 'base.html.twig' %}

{% block title %}CeramicCategory{% endblock %}

{% block body %}
    <h1>CeramicCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ceramic_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ceramic_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ceramic_category_index') }}">back to list</a>

    <a href="{{ path('ceramic_category_edit', {'id': ceramic_category.id}) }}">edit</a>

    {{ include('ceramic_category/_delete_form.html.twig') }}
{% endblock %}
